# 万象设计系统 · Draft 0.1

## 设计意图

万象不是魔法玩具，也不是开发者 IDE 的仿制品。界面应该像一张清晰、可信、可共同批注的工作台：让普通人理解正在构建什么，让导师能看见证据，让系统对风险保持诚实。

## Color

- `Paper` — `#F4F1EA`：主背景，温暖但不轻浮。
- `Ink` — `#18201F`：主要文字与高对比结构。
- `Slate` — `#5F6966`：说明文字和辅助信息。
- `Line` — `#CFD2C9`：分隔线和边界。
- `Jade` — `#1F6B57`：唯一强调色，只用于状态与关键进度。
- `White` — `#FFFEFA`：内容面板。

## Typography

- 展示与标题：`Noto Serif SC`, `Songti SC`, serif。强调思考、判断和长期积累。
- 正文与界面：`Inter`, `PingFang SC`, sans-serif。保证工作信息的读取效率。
- 数据与标签：`IBM Plex Mono`, `SFMono-Regular`, monospace。用于状态、版本、指标。

## Layout

- 桌面优先，最大内容宽度 `1180px`。
- 12 列网格；主要内容区使用 7/5 或 8/4 分栏。
- 使用细线、留白和排版建立层级，不堆叠装饰性卡片。
- 移动端按阅读顺序折叠为单列。

## Components

- `Status pill`：只表达真实状态，不用于装饰。
- `Evidence table`：指标必须包含目标、测量方式与时间窗口。
- `Milestone rail`：展示从发现到真实运行的门槛。
- `Decision block`：记录决定、原因、负责人和复审时间。
- `Run proof`：一次工作运行的输入、过程、输出、批准与结果。

## Interaction

- 默认展示结果与下一步；过程证据按需展开。
- 高风险写操作必须明确确认，并展示将影响的数据。
- 不使用“魔法”“瞬间完成”等措辞；使用“验证”“运行”“批准”“发布”。
- 动效只用于状态变化和流程推进，并遵守 `prefers-reduced-motion`。
